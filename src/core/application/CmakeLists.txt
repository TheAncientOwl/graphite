add_library(graphite_core_application SHARED
    GraphiteApplication.cpp
)

target_link_libraries(graphite_core_application PRIVATE
    graphite_core_logger
    graphite_core_renderer
)

target_include_directories(graphite_core_application PUBLIC
    ${PROJECT_SOURCE_DIR}/src
)
